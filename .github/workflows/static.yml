# Workflow disabled: use deploy.yml
name: Deploy static content to Pages (disabled)

on:
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - run: echo "workflow disabled"
